
Para tocar uma música, ou trocar de música, use o comando
obj_sound_controller.play_music( "nome da música" );

E para parar use:
obj_sound_controller.stop_music( );


Só isso, o fade e o restando o proprio obj sound controller já cuida disso.